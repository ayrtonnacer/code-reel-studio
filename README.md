# CodeSnap Video

Convert code snippets into vertical 9:16 videos (1080×1920, 30 FPS) for TikTok, Reels, and Shorts. Everything runs client-side — no backend needed.

## Development

```bash
npm install
npm run dev     # http://localhost:8080
npm run build
npm run test
```
